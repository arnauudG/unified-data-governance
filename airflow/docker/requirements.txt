python-dotenv==1.0.1
pandas>=2.2.2,<2.4
pyarrow>=15.0.0,<22.0.0
setuptools
Faker
# Collibra integration dependencies
requests>=2.31.0
pyyaml>=6.0
# Soda Library for Snowflake (with template support)
# Install with: pip install -i https://pypi.cloud.soda.io soda-snowflake==1.12.24
# Note: soda-snowflake is installed separately from Soda's private PyPI with pinned version
soda-snowflake==1.12.24
# dbt and adapter (updated for protobuf compatibility)
dbt-core==1.10.11
dbt-snowflake==1.10.2
# Pin critical dependencies to avoid conflicts
# Using protobuf>=6.30.0,<6.31.0 to satisfy dbt (>=6.0,<7.0) while minimizing conflicts
# Note: protobuf 6.0.0-6.29.x don't exist, first 6.x version is 6.30.0
protobuf>=6.30.0,<6.31.0
pydantic>=2.5.2,<3.0.0
# Upgrade conflicting packages to support protobuf 6.x
google-api-core>=2.23.0
googleapis-common-protos>=1.66.0
proto-plus>=1.26.0
# PostgreSQL connector
psycopg2-binary>=2.9.0